# JSEclaw VPS Quickstart

```bash
git clone https://github.com/Ericnussa/JSEclaw.git
cd JSEclaw
pnpm install
pnpm build
node jseclaw.mjs onboard --install-daemon
```

## Command note
Use `jseclaw` as the preferred command name going forward.
`openclaw` remains available for compatibility.
